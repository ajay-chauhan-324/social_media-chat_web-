# Base URL for the API. Leave as-is to use the Vite dev proxy (recommended in dev).
# In production point this at your deployed backend, e.g. https://api.artroot.chat
VITE_API_URL=/api
VITE_SOCKET_URL=http://localhost:5000
